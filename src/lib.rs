#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_compiles() {
        // 简单测试确保项目可以编译
        assert!(true);
    }

    #[test]
    fn test_config_structure() {
        // 测试配置值是否正确设置
    }
}
